apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-domivice-dev-cert
  namespace: domivice-k8s-dashboard
spec:
  dnsNames:
    - k8s.domivice.dev
  secretName: k8s-domivice-dev-tls-secret
  issuerRef:
    name: domivice-cluster-issuer
    kind: ClusterIssuer